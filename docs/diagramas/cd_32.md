@startuml
title 3.2 — Contexto Técnico (Diagrama de Componentes)

skinparam componentStyle rectangle

actor "Navegador (Cliente Web)" as Browser

node "Servidor de Aplicação" {

  component "Nginx\n(Proxy Reverso + TLS)" as Nginx
  component "Gunicorn\n(WSGI Server)" as Gunicorn
  component "Aplicação Flask\n(Blueprints + Services)" as FlaskApp
  component "SQLAlchemy ORM" as ORM

}

node "Servidor de Banco de Dados" {
  database "PostgreSQL 14\n(Dados + Audit Log)" as DB
}

node "Serviço Externo" {
  component "Servidor SMTP\n(E-mail)" as SMTP
}

Browser --> Nginx : HTTPS
Nginx --> Gunicorn : Proxy (HTTP/Unix Socket)
Gunicorn --> FlaskApp : WSGI
FlaskApp --> ORM : Operações ORM
ORM --> DB : SQL (TCP)

FlaskApp --> SMTP : SMTP/TLS

@enduml